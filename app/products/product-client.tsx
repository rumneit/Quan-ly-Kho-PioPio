"use client";
import Link from "next/link";import {FormEvent,useMemo,useState} from "react";import type {Profile} from "@/lib/auth";
type Product={id:string;name:string;sku:string;price:number;stock_quantity:number;active:boolean};const money=(n:number)=>new Intl.NumberFormat("vi-VN").format(n);
export default function ProductClient({profile,initialProducts}:{profile:Profile;initialProducts:Product[]}){const [products,setProducts]=useState(initialProducts),[q,setQ]=useState(""),[open,setOpen]=useState(false),[saving,setSaving]=useState(false);const rows=useMemo(()=>products.filter(p=>`${p.name} ${p.sku}`.toLowerCase().includes(q.toLowerCase())),[products,q]);async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);const r=await fetch("/api/products",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const d=await r.json();setSaving(false);if(r.ok){setProducts(x=>[d.product,...x]);setOpen(false)}}return <div className="kv-shell product-page"><header className="kv-header"><Link className="kv-brand" href="/dashboard"><span className="kv-brand-symbol"><i/><i/></span><strong>PioPio</strong></Link></header><nav className="kv-horizontal-nav"><div className="kv-horizontal-items"><Link className="kv-top-item" href="/dashboard">Tổng quan</Link><Link className="kv-top-item active" href="/products">Hàng hóa</Link></div><Link className="kv-sale-link" href="/sales">🛒 Bán hàng</Link></nav><main className="products-main"><div className="products-heading"><div><p>Hàng hóa</p><h1>Danh sách hàng hóa</h1></div><button onClick={()=>setOpen(true)}>+ Thêm hàng hóa</button></div><section className="products-card"><div className="products-toolbar"><label>⌕<input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm theo mã, tên hàng"/></label><select><option>Tất cả trạng thái</option><option>Đang kinh doanh</option></select><button>☷ Bộ lọc</button><span/><button>⇩ Xuất file</button></div><div className="products-count">Tổng cộng: <b>{rows.length}</b> hàng hóa</div><div className="products-table"><table><thead><tr><th>Mã hàng</th><th>Tên hàng</th><th>Giá bán</th><th>Tồn kho</th><th>Trạng thái</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td>{p.sku}</td><td><b>{p.name}</b></td><td>{money(Number(p.price))}</td><td>{p.stock_quantity}</td><td><em>{p.active?"Đang kinh doanh":"Ngừng kinh doanh"}</em></td></tr>)}{!rows.length&&<tr><td colSpan={5}><div className="products-empty">▤<strong>Chưa có hàng hóa</strong><p>Thêm hàng hóa đầu tiên để bắt đầu quản lý kho.</p><button onClick={()=>setOpen(true)}>Thêm hàng hóa</button></div></td></tr>}</tbody></table></div><footer>Hiển thị {rows.length} hàng hóa <span>‹ 1 ›</span></footer></section></main>{open&&<div className="modal open"><button className="modal-backdrop" onClick={()=>setOpen(false)}/><form className="modal-card" onSubmit={add}><div className="modal-head"><h3>Thêm hàng hóa</h3><button type="button" className="close-btn" onClick={()=>setOpen(false)}>×</button></div><div className="form-grid"><label className="full">Tên hàng<input name="name" required/></label><label>Mã hàng<input name="sku" required/></label><label>Giá bán<input name="price" type="number" min="0" required/></label><label>Tồn kho<input name="stock" type="number" min="0" defaultValue="0"/></label></div><div className="modal-actions"><button type="button" className="button secondary" onClick={()=>setOpen(false)}>Hủy</button><button className="button primary" disabled={saving}>{saving?"Đang lưu...":"Lưu"}</button></div></form></div>}</div>}
