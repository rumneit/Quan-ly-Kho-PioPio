"use client";
import Link from"next/link";import{useMemo,useState}from"react";import type{Profile}from"@/lib/auth";
type P={id:string;name:string;sku:string;price:number;stock_quantity:number;active:boolean};const m=(n:number)=>new Intl.NumberFormat("vi-VN").format(n);const nav=["Mua hàng","Đơn hàng","Khách hàng","Nhân viên","Sổ quỹ","Báo cáo","Bán online","Thuế & Kế toán"];
export default function ProductClient({profile,initialProducts}:{profile:Profile;initialProducts:P[]}){const[q,setQ]=useState(""),[products]=useState(initialProducts);const rows=useMemo(()=>products.filter(p=>`${p.name} ${p.sku}`.toLowerCase().includes(q.toLowerCase())),[products,q]);const initials=profile.full_name.split(" ").map(x=>x[0]).slice(-2).join("");return <div className="kv-shell product-page"><header className="kv-header"><Link className="kv-brand" href="/dashboard"><span className="kv-brand-symbol"><i/><i/></span><strong>PioPio</strong></Link><div className="kv-header-actions"><button className="kv-round">🔔</button><button className="kv-round">⚙</button><button className="kv-avatar">{initials}</button></div></header><nav className="kv-horizontal-nav"><div className="kv-horizontal-items"><Link className="kv-top-item" href="/dashboard">Tổng quan</Link><Link className="kv-top-item active" href="/products">Hàng hóa</Link>{nav.map(x=><span className="kv-top-item" key={x}>{x}</span>)}</div><Link className="kv-sale-link" href="/sales">🛒 Bán hàng</Link></nav><main className="products-main"><div className="products-heading"><div><p>Hàng hóa</p><h1>Danh sách hàng hóa</h1></div><Link className="products-add" href="/dashboard">+ Thêm hàng hóa</Link></div><section className="products-card"><div className="products-toolbar"><label>⌕<input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm theo mã, tên hàng"/></label><select><option>Tất cả trạng thái</option></select><button>☷ Bộ lọc</button><span/><button>⇩ Xuất file</button></div><div className="products-count">Tổng cộng: <b>{rows.length}</b> hàng hóa</div><div className="products-table"><table><thead><tr><th>Mã hàng</th><th>Tên hàng</th><th>Giá bán</th><th>Tồn kho</th><th>Trạng thái</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td>{p.sku}</td><td><b>{p.name}</b></td><td>{m(Number(p.price))}</td><td>{p.stock_quantity}</td><td><em>{p.active?"Đang kinh doanh":"Ngừng kinh doanh"}</em></td></tr>)}{!rows.length&&<tr><td colSpan={5}><div className="products-empty">▤<strong>Chưa có hàng hóa</strong><p>Thêm hàng hóa đầu tiên để bắt đầu quản lý kho.</p></div></td></tr>}</tbody></table></div><footer>Hiển thị {rows.length} hàng hóa <span>‹ 1 ›</span></footer></section></main></div>}
